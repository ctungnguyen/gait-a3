# Training checkpoints

`train_rotation.py`, `train_direct.py`, and `train_both.py` write periodic PPO
checkpoints beneath this directory. Keep useful checkpoints while training;
only the final canonical models under `models/` are mandatory submission files.
