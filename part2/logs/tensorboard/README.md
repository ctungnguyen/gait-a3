# TensorBoard logs

Final training creates separate event files beneath `rotation/` and `direct/`.
The assignment requires these generated logs to be included in the repository
ZIP submitted by the team.
