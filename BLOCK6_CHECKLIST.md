# Part II Block 6 — deep-RL completion checklist

## Code implementation

- [x] Stable Baselines3 PPO selected consistently for both agents
- [x] `MlpPolicy` uses at least one hidden layer (`[128, 128]` baseline)
- [x] Headless training with no Pygame rendering
- [x] Four seeded training environments
- [x] Separate rotation/thrust and direct-direction model outputs
- [x] Separate visual evaluation entry points
- [x] Periodic deterministic evaluation on a separate environment
- [x] Best-model selection plus final checkpoint
- [x] Checkpoints for interrupted-run recovery
- [x] TensorBoard logging
- [x] Monitor episode CSV logs
- [x] Custom TensorBoard metrics for actions, events, reward components, phase,
  health, enemies, and spawners
- [x] Three meaningful hyperparameter presets
- [x] Measured tuning runner and ranking output
- [x] Final headless evaluation with reproducible seeds
- [x] Fair two-style comparison on identical episode seeds
- [x] Training-curve PNG generation
- [x] Model metadata with action contract, observation labels, settings,
  dependency versions, evaluation metrics, and verified SHA-256 checksum
- [x] Strict submission verifier

## Required generated evidence

These boxes become complete only after running the training pipeline on a
machine with Stable Baselines3 and PyTorch. Source code alone cannot honestly
satisfy these evidence requirements.

- [ ] Hyperparameter trial CSV and JSON generated
- [ ] `models/rotation_agent.zip` generated by PPO training
- [ ] `models/direct_agent.zip` generated by PPO training
- [ ] Matching metadata JSON exists for both models
- [ ] TensorBoard event files exist for both styles
- [ ] Monitor and periodic evaluation logs exist for both styles
- [ ] Final evaluation summaries and episode CSVs exist for both styles
- [ ] Combined comparison CSV and JSON generated
- [ ] Combined training-curve PNG generated
- [ ] Both trained models visually evaluated in Pygame
- [ ] Both trained-agent clips recorded for the video
- [ ] Saved models and TensorBoard logs included in the submitted repository ZIP

Run `python verify_block6_submission.py` after training. A strict PASS means the
expected Block 6 artifacts are present and match their control contracts.

## Report evidence to extract

- PPO choice and why the same algorithm was used for a fair comparison
- Fixed 25-value observation vector
- Exact `Discrete(5)` versus `Discrete(6)` mappings
- Two-hidden-layer MLP architecture
- Final reward table and justification
- The three measured tuning configurations and selection rule
- TensorBoard/evaluation learning curves
- Mean ± standard deviation over repeated seeded evaluations
- Success rate, phase reached, damage, destroyed entities, and episode length
- Limitations and any unsuccessful behaviours observed
